# pure-proxy-ah-trapped

It queries the pure-proxy accounts that exist on the Polkadot Relay-Chain which have sufficients on Polkadot Asset Hub.

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```
